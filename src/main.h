#pragma once

extern Window *s_main_window;

extern GColor default_color;
extern GColor default_bg_color;

extern bool enable_second;


enum {
	KEY_JS_READY = 0,
	KEY_FUNC,
	KEY_WEATHER,
	KEY_WEATHER_TEMPERATURE,
	KEY_WEATHER_CONDITIONS
};
